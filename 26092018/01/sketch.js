//creare un array di 5 cerchi.
ogni cer

function setup(){
    createCanvas(640, 480);
    background(0);

    const size = 30;
    ellipse(100, 100, size, size);
}

function draw(){
    // const size = 30;
    // ellipse(100, 100, size, size);
}

//array buoto
var arrayCerchi = [];
//funz che popola l'array
var creaCerchio = function(x, y, array){
    
}
//chiamarla 5 volte.

var cerchio = {
    x: 5,
    y: 7,
    radius: 22
}